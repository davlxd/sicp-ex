; As hint suggested, if Process P1 needs to access Resource R1 to know later it needs to access R2,
; and P2 needs to access R2 to know next it needs to access R1
;
; In this case we cannot force P1 and P2 to access R1 first then R2


; End of S3.4, IMHO this section is relatively sloppy, so are my ex solutions.
;
